{
  "name": "stablestabler",
  "private": true,
  "workspaces": [
    "apps/backend"
  ],
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "build": "npm run build -w apps/backend",
    "start": "concurrently -n BACKEND -c green \"npm run start -w apps/backend\"",
    "start:dev": "concurrently -n BACKEND -c green \"npm run start:dev -w apps/backend\"",
    "prod:build": "npm run build -w apps/backend",
    "prod:start": "npm run start:prod -w apps/backend",
    "format": "echo \"No frontend: format skipped\"",
    "lint": "echo \"No frontend: lint skipped\"",
    "clean": "npm run clean -w apps/backend && rm -rf node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  }
}
